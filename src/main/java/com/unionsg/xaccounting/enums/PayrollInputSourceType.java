package com.unionsg.xaccounting.enums;

/** The kind of variable (non-recurring) payroll input being recorded for an employee/period. */
public enum PayrollInputSourceType {
    OVERTIME,
    BONUS,
    COMMISSION,
    ALLOWANCE,
    REIMBURSEMENT,
    DEDUCTION,
    ADJUSTMENT,
    ARREARS
}
